# Frontend Environment Variables
# Copy this file to .env.local for local development
#
# NOTE: When deploying with Azure Developer CLI (azd), these values are
# automatically configured via:
# 1. preprovision hooks create Entra ID app registrations
# 2. Values are exported to azd environment (VITE_* variables)
# 3. azure.yaml passes these as Docker build args
# 4. Vite embeds them into the build at compile time

# ===========================================
# Azure Entra ID / MSAL Configuration
# ===========================================

# The SPA app registration client ID (GUID)
# Automatically set by: azd env set VITE_AZURE_CLIENT_ID
VITE_AZURE_CLIENT_ID=

# The Azure AD tenant ID
# Automatically set by: azd env set VITE_TENANT_ID
VITE_TENANT_ID=

# Space-separated API scopes for backend API access
# Example: "api://<api-app-id>/Decks.ReadWrite api://<api-app-id>/Cards.ReadWrite"
# Automatically set by: azd env set VITE_API_SCOPE
VITE_API_SCOPE=

# Optional: Override redirect URI (defaults to window.location.origin)
# VITE_REDIRECT_URI=http://localhost:3000

# ===========================================
# Authentication Toggle
# ===========================================
# Set to "false" to disable authentication for local development
# When disabled, the app uses X-User-Id header instead of Bearer tokens
VITE_AUTH_ENABLED=true

# ===========================================
# API Configuration
# ===========================================
# Backend API URL (optional, defaults to /api which is proxied)
# - In development: Vite proxies /api to backend (see vite.config.ts)
# - In production: Nginx proxies /api to internal backend container
# VITE_API_URL=
